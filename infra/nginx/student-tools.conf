# ==============================================================================
# NovaShop DevOps Platform — Nginx Edge Reverse Proxy Şablonu
# ==============================================================================

# HTTP -> HTTPS Yönlendirmesi
server {
    listen 80;
    server_name ~^(?<student>student\d+)-(novashop|gitlab|harbor|sonarqube|jenkins)\.devopsatolyesi\.com$;
    return 301 https://$host$request_uri;
}

# 1. NovaShop Web UI -> Port 8888
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-novashop\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 2. GitLab CE -> Port 8929
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-gitlab\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 250M;

    location / {
        proxy_pass http://127.0.0.1:8929;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 3. Harbor OCI Registry -> Port 18082
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-harbor\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 0;

    location / {
        proxy_pass http://127.0.0.1:18082;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_buffering off;
        proxy_request_buffering off;
    }
}

# 4. SonarQube -> Port 19000
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-sonarqube\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:19000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 5. Jenkins Controller -> Port 18080 (WebSocket Destekli)
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-jenkins\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    client_max_body_size 256M;

    location / {
        proxy_pass http://127.0.0.1:18080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 6. NovaShop Kubernetes UI (LAB-06) -> Port 30080
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-k8s\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:30080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 7. Prometheus (LAB-10) -> Port 9090
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-prometheus\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:9091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 8. Grafana (LAB-10) -> Port 3000
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-grafana\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 9. Jaeger Tracing (LAB-10) -> Port 16686
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-jaeger\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:16686;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}

# 10. Kibana Log Analitiği (LAB-11) -> Port 5601
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-kibana\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:5601;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# 11. Elasticsearch API (LAB-11) -> Port 9200
server {
    listen 443 ssl http2;
    server_name ~^(?<student>student\d+)-elastic\.devopsatolyesi\.com$;

    ssl_certificate /etc/nginx/ssl/devops-training/origin.crt;
    ssl_certificate_key /etc/nginx/ssl/devops-training/origin.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:9200;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
    }
}
